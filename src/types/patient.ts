/**
 * Patient interface defines the structure of patient data
 */
export interface Patient {
  id: number;
  name: string;
  age: number;
  gender: string;
  phone: string;
  email: string;
  lastVisit: string;
  condition: string;
  status: string;
}
